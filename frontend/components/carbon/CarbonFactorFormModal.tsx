"use client";
import { zodResolver } from "@hookform/resolvers/zod"; import { useForm } from "react-hook-form"; import { z } from "zod";
import { ModalShell } from "@/components/common/ModalShell"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { carbonCategories } from "@/components/carbon/CarbonFilters"; import type { CarbonFactor, CarbonFactorCreate, CarbonFactorUpdate } from "@/types/carbon";
const schema = z.object({ name: z.string().min(1), category: z.string().min(1), unit: z.string().min(1), factor_value: z.coerce.number().min(0.000001), source: z.string().optional(), source_url: z.string().url("URL invalida").optional().or(z.literal("")), year: z.coerce.number().min(1900).max(new Date().getFullYear() + 1).optional(), is_active: z.boolean().optional() });
export function CarbonFactorFormModal({ factor, loading, onClose, onSubmit }: { factor?: CarbonFactor | null; loading?: boolean; onClose: () => void; onSubmit: (data: CarbonFactorCreate | CarbonFactorUpdate) => Promise<void> }) {
  const { register, handleSubmit, formState: { errors } } = useForm<z.infer<typeof schema>>({ resolver: zodResolver(schema), defaultValues: { name: factor?.name || "", category: factor?.category || "ENERGY", unit: factor?.unit || "kWh", factor_value: Number(factor?.factor_value ?? factor?.factor_kgco2e ?? 0), source: factor?.source || "", source_url: factor?.source_url || "", year: factor?.year || new Date().getFullYear(), is_active: factor?.is_active ?? true } });
  return <ModalShell title={factor ? "Editar factor" : "Crear factor"} onClose={onClose}><form className="space-y-4" onSubmit={handleSubmit((v) => onSubmit({ ...v, source: v.source || null, source_url: v.source_url || null }))}><div className="grid gap-3 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Nombre<Input {...register("name")} /></label><label className="grid gap-2 text-sm font-semibold">Categoria<select className="h-12 rounded-2xl border px-4" {...register("category")}>{carbonCategories.map((c) => <option key={c.value} value={c.value}>{c.label}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold">Unidad<Input {...register("unit")} /></label><label className="grid gap-2 text-sm font-semibold">Factor<Input type="number" step="0.000001" {...register("factor_value")} />{errors.factor_value ? <span className="text-xs text-rose-600">{errors.factor_value.message}</span> : null}</label><label className="grid gap-2 text-sm font-semibold">Fuente<Input {...register("source")} /></label><label className="grid gap-2 text-sm font-semibold">URL fuente<Input {...register("source_url")} />{errors.source_url ? <span className="text-xs text-rose-600">{errors.source_url.message}</span> : null}</label><label className="grid gap-2 text-sm font-semibold">Año<Input type="number" {...register("year")} /></label><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" {...register("is_active")} />Activo</label></div><div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={onClose}>Cancelar</Button><Button disabled={loading}>Guardar</Button></div></form></ModalShell>;
}
